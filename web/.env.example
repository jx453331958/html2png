# Server
PORT=3000
HOST=0.0.0.0

# JWT Secret (REQUIRED in production, minimum 32 characters)
# Generate with: openssl rand -hex 32
JWT_SECRET=

# Admin Account (optional, for initial setup)
ADMIN_EMAIL=
ADMIN_PASSWORD=

# HTML Content Encryption (optional but recommended)
# If set, HTML content will be encrypted in database
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
