# Server
PORT=3000
HOST=0.0.0.0

# Admin Account (optional, for initial setup)
# If set, an admin account will be created on first startup
ADMIN_EMAIL=
ADMIN_PASSWORD=

# HTML Content Encryption (optional)
# If set, HTML content will be encrypted in database
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
